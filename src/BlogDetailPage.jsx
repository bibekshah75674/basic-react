import React from 'react'
import { useParams } from 'react-router-dom'
import BlogPage from './assets/BlogPage';

const BlogDetailPage = () => {
    // const {id} = useParams();
    // const mainBlogs = blogs.find((item) =>(
    //   item.id === parseInt(id)
    // ))
    // console.log(mainBlogs);
  return (
    <>
    <h1>Hi</h1>
    </>
  )
}

export default BlogDetailPage

